---
name: skill-name
description: >
  用一句话说明这个 skill 什么时候触发、解决什么任务、产出什么结果。描述要足够具体，让 Agent 在用户没有显式点名 skill 时也能判断该不该使用。
---

# Skill Title

说明这个 skill 的目标、适用场景和默认产出。

## 使用场景

- 用户会怎么描述这个需求
- 哪些上下文应该触发
- 哪些相近需求不应该触发

## 默认流程

1. 第一步
2. 第二步
3. 第三步

## 输入

说明需要从用户、上下文、文件或工具里读取什么。

## 输出

说明最终应该写文件、返回文本、提交代码，还是只给建议。

## 校验

说明完成后要跑哪些检查，或者至少如何做最低限度自检。

